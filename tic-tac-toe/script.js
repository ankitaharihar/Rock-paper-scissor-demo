let board = ["", "", "", "", "", "", "", "", ""];
const cells = document.querySelectorAll(".cell");
console.log(cells);
let currentPlayer = "X";
let gameActive = true;
const winningConditions = [
  [0, 1, 2],
  [3, 4, 5],    
    [6, 7, 8],
];

cells.forEach(function(cell){

});
cell.addEventListener(cell)
function handleCellClick(event){

}